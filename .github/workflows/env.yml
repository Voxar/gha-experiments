name: Are env vars exported
on:
  push

jobs: 
  job: 
    runs-on: ubuntu-latest
    steps: 
      - name: "Will it export"
        env: 
          MESSAGE: "Hello World"
        run: |
          sh -c 'echo \"Your message is $MESSAGE\"'